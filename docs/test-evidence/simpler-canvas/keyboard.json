{
  "checks": [
    "After selecting a tree type, A on the focused type chip opens a child draft; saving preserves the method and hidden notes. Counts read 1 thought and 2 thoughts.",
    "Enter and Space on + directly focus an unsaved child draft; discard writes nothing, and arrow keys do not become card-navigation commands.",
    "Shift+A adds a sibling draft, E edits the owning thought and T opens its tree type from a focused card control, without implicit saves.",
    "Shortcut letters and plain Enter remain text while typing; A on another card flushes the original edit before starting a separate child draft.",
    "The same letter shortcuts work from the collapse control."
  ],
  "errors": []
}
